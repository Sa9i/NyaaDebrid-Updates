# NyaaDebrid Updates

Public update channel for the private NyaaDebrid source repository.

This repository intentionally contains only release metadata and compiled Windows update packages. It does **not** contain the application source code or API credentials.

The application reads `update.json` to discover new versions, verify the SHA-256 checksum, and install updates.
